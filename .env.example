# Anthropic (required for generation)
ANTHROPIC_API_KEY=

# Supabase (optional — enables shareable /plan/[slug] links)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Public site URL for share links (optional; falls back to Vercel URL or localhost)
NEXT_PUBLIC_APP_URL=
